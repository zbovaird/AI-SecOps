# Red Team Testing Kit Requirements

# Core dependencies
asyncio>=3.4.3
typing-extensions>=4.0.0

# Utilities
python-dotenv>=1.0.0
pyyaml>=6.0

# Network and HTTP
requests>=2.31.0

# AI/ML Dependencies (for Whitebox Attacks)
torch>=2.0.0
transformers>=4.30.0
adversarial-robustness-toolbox>=1.15.0
numpy>=1.24.0

# Console Interface
cmd2>=2.4.0  # Optional: For enhanced console features if we upgrade later

# DNS operations
dnspython>=2.3.0

# Optional: For advanced features
# cryptography>=41.0.0  # For encoding/encryption
# python-nmap>=0.7.1    # For advanced port scanning
# paramiko>=3.0.0       # For SSH operations
# ldap3>=2.9.0          # For LDAP/Active Directory operations
# impacket>=0.10.0      # For Windows domain operations (requires build)
# mimikatz              # For Windows credential extraction (Windows-only binary)
# procdump              # For Windows memory dumping (Windows-only binary)

